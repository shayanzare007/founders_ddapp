You need to set up a setup_env.sh file that exports the following environment variables:

DATA_DIR
APP_HOME

filter.cpp is used to filter the lines of a file before copying to a database. To compile:
g++ filter.cpp -o filter
